import { ResortTypeFilterPipe } from './resort-type-filter.pipe';

describe('ResortTypeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ResortTypeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
